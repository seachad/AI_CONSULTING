# AI_CONSULTING · Instrucciones para Claude

Repositorio de metodologías de IA de Fernando García Varela (D81). El trabajo principal es **SEVEN-G**.

## Antes de trabajar en SEVEN-G

1. Lee el registro de decisiones (importado abajo). Es la fuente de verdad sobre lo decidido.
2. Lee `SEVEN-G/mds/es/_trabajo/20260916_Analisis_estado_y_huecos_SEVEN-G.md` para el plan documental, el estado de cada documento y el orden de trabajo.
3. Los documentos vigentes están en `SEVEN-G/mds/es/` (redacción) y `SEVEN-G/mds/en/` (traducción). `SEVEN-G/_legacy/` no es vigente.
4. **SPHERES** y **SPAD** (metodologías de apoyo, en construcción) viven en `SPHERES/` y `SPAD/` con la misma jerarquía (`mds`, `html`, `pdf`, `build/componentes`, `_legacy`). SPHERES es la capa explicativa de las esferas y niveles; las reglas operativas están en SEVEN-G (documento 10). Un cambio en esferas, niveles o grados en SEVEN-G se revisa también en SPHERES y viceversa (D42).

## Gestión de la memoria del proyecto

- **Toda decisión nueva** del autor se añade de inmediato a `.claude/seveng_decisiones.md` (fecha, decisión, motivo, estado). Si sustituye a otra, se indica.
- Al terminar o actualizar un documento, se actualiza su estado en el plan documental (`_trabajo`) y en el catálogo de herramientas (documento 03) si afecta a herramientas.
- Lo que haya que recordar y no sea una decisión (pendientes, dudas abiertas, preferencias) se anota en la sección "Pendientes" de este fichero.

## Convenciones de trabajo

- La fuente de verdad es el Markdown (`SEVEN-G/mds/es/` y `SEVEN-G/mds/en/`). Los HTML y PDF se generan siempre desde esos ficheros; no se editan en paralelo ni se quedan desincronizados.
- Generar siempre con `pwsh -File SEVEN-G/build/build.ps1` (SEVEN-G, SPHERES y SPAD, ambos idiomas, HTML y PDF; `-Metodologias SPHERES` para una sola). La primera generación de un documento nuevo se ejecuta dos veces para que cada idioma enlace el PDF del otro. Revisar visualmente el resultado antes de entregarlo: capturas con Edge sin ventana y `SEVEN-G/build/pdf_a_png.ps1` (ejecutar con `powershell.exe` 5.1 y rutas de salida cortas).
- Todo cambio en un documento en un idioma debe replicarse en su versión del otro idioma en la misma entrega. Si hay una edición en español, el inglés debe quedar sincronizado con la misma estructura, contenido y alcance; no se deja un idioma atrasado.
- Los HTML deben incluir una zona de descargas con enlaces a los PDF equivalentes del mismo idioma y, cuando corresponda, a la versión equivalente del otro idioma. Los enlaces deben ser explícitos y visibles.
- Los HTML contienen enlaces de descarga para los PDFs equivalentes: español a PDFs en español e inglés a PDFs en inglés; no se cruzan los enlaces entre idiomas salvo como referencia explícita de la versión paralela.
- Cada HTML muestra bajo la portada la zona de descargas (PDF propio, versión del otro idioma y herramientas relacionadas) (D39).
- SEVEN-G v0.1 es operativa: no escribir "previsto", "en construcción" ni "por construir" referido a SEVEN-G (D40).
- Toda referencia externa lleva enlace a su fuente oficial, registrado y verificado en el registro de referencias; nada de fuentes secundarias ni referencias sin verificar (D41).
- Traducciones al inglés según `SEVEN-G/build/guia_traduccion_en.md` (reglas y glosario obligatorio).
- Convenciones del Markdown (portada, `<!-- cifras: -->`, `<!-- figura: -->`, `<!-- grafico: -->`, Mermaid): ver cabecera de `SEVEN-G/build/build.ps1`.
- En el equipo no hay Python, Node ni pandoc: usar PowerShell 7 y Edge.
- Revisión interactiva de los HTML (búsqueda, índice, tema, idioma): `pwsh -File SEVEN-G/build/servidor.ps1` y abrir `http://localhost:8765/` (portada), `/SEVEN-G/html/es/`, `/SPHERES/html/es/` o `/SPAD/html/es/`.
- **Todo panel o herramienta, existente o nuevo, hace navegables los códigos que nombra** (D99): carga `SEVEN-G/html/<idioma>/codigos.js`, lleva en su navegación un elemento `data-ir-codigo` (que da el «Buscador de documentos» —antes «Ir a código», D105— y «Citados aquí») y marca su contenido con `data-enlazar-codigos` (cada código escrito pasa a ser enlace con tooltip, también en lo que se dibuja después). En un panel generado por T17 se activa con `navegacion.codigos` en `config_panel.json`. Si el panel promete no medir visitas (comunidad, paneles del consejo), la etiqueta `<script>` lleva `data-sin-medicion`. `verificar_coherencia.ps1` (17) lo comprueba.
- El buscador de términos de la barra de los documentos busca en esta página o en todo el sitio (D105): el índice de texto `SEVEN-G/html/<idioma>/busqueda.json` lo genera `SEVEN-G/build/busqueda.ps1` desde los HTML (lo llama `build.ps1` tras `codigos.ps1`); no se edita a mano y `verificar_coherencia.ps1` (20) lo comprueba.

## Cierre de cada tarea

- Trabajar con las recomendaciones propias sin pedir confirmación paso a paso y **terminar siempre con commit y push** a `main` si pasan las pruebas de coherencia: `pwsh -File SEVEN-G/build/verificar_coherencia.ps1` (código de salida 0). Si fallan, corregir o explicar; no publicar (D52).
- **No romper nada hacia atrás** salvo petición expresa del autor: lo nuevo se añade como opción o configuración, y los datos y ficheros existentes siguen funcionando (D53).
- Cuando una decisión nueva sea comprobable de forma automática, añadir su comprobación a `verificar_coherencia.ps1`.

## Parámetros comunes y coherencia

- Antes de redactar o modificar un documento, leer `SEVEN-G/mds/es/_trabajo/20260916_Especificacion_comun_biblioteca_SEVEN-G.md` (escalas, códigos, nombres de fichero, estructura) y `_trabajo/20260916_Ajustes_de_coherencia_SEVEN-G.md` (reglas R1–R15 que ya aplican todos los documentos).
- El glosario oficial es el documento 02; la guía de traducción, `SEVEN-G/build/guia_traduccion_en.md`.
- Todo cambio en un documento español se replica en su versión inglesa en la misma entrega (D12).

## Pendientes

- 22-09-2026 (D106): el Excel del caso de T06 (pestaña Riesgos de la ficha de T01) toma sus propuestas de `T01_registro_iniciativas/catalogo_riesgos.json` y el texto de cada riesgo tipo del documento 33 §9: **si se añade, quita o renombra un código RT en el documento 33 (ES o EN), hay que dar de alta o baja su entrada en el catálogo** (`build_registro.ps1` falla si no coinciden) y regenerar `registro.html`. Las reglas de aplicabilidad (`aplica`) y los textos de mitigación y contingencia son propuestas de Claude: pendiente del autor validarlas y calibrar qué riesgos tipo deben aplicar a cada perfil (hoy, un agente A2 Enterprise recibe 49 riesgos tipo a considerar; la hoja los ordena con la columna «Cuándo revisar» según la fase en que el documento 33 sitúa cada uno frente a la fase del caso). Las hojas siguen la estructura de P13 §3–§6; si cambia P13, revisar las columnas del generador (`xlsxCaso` en la plantilla de T01).
- 22-09-2026 (D103): los datos de cada persona o compañía viven fuera del sitio (navegador, fichero del equipo o `SEVEN-G/herramientas/datos/` en la copia de la compañía, documento 03 §2.1). El módulo `herramientas/_comun/datos_locales.js` se incrusta al generar cada herramienta (`build_*.ps1`, marca `__DATOS_LOCALES__`): si cambia, regenerar las cuatro. **La carpeta `herramientas/datos/` no puede contener ningún JSON en el repositorio** (solo su README; lo comprueba la sección 19). El conector T01 → panel existe en dos lenguajes con una sola correspondencia: `T17_panel_consejo/t01_a_panel.py` y `t01_a_panel.js` (incrustado en los paneles y en T01; el panel se regenera en el navegador sin Python). **Todo cambio de mapeo se hace en los dos** y la sección 19b de la verificación falla si divergen con los datos de demostración; tras cambiar el `.js` hay que regenerar el panel de ejemplo (`uv run python t01_a_panel.py`) y T01. Pendiente del autor: probar una vez el guardado en fichero con un fichero real (usa el diálogo nativo del navegador y no se pudo automatizar) y decidir si la vista sin JavaScript de los paneles debe regenerarse también en el navegador (hoy es la de los datos incrustados al generar).
- 22-09-2026 (D100): el registro T01 es la fuente de verdad de T11, T14, T15 y T17 (mapa en `SEVEN-G/herramientas/mapa_datos.json` y documento 03 §4.1; si cambia el esquema de T01 o lo que lee una herramienta, se actualizan los tres). Si cambian los datos de demostración de T01: regenerar `T17_panel_consejo/ejemplo/t14_indice.json` (`build_indice.ps1 -DesdeT01 … -Exportar …`), copiar ese cálculo a `T14/datos_demo.json` (origen `t01`), regenerar la lista `madurez[]` de T01 con `build_madurez.ps1 -Resumen …` si cambian las evaluaciones de T15, y volver a generar registro, calculadoras y panel. Regla de precedencia confirmada y refresco de T11 desde T01 hecho (D102). La rama `datos-locales-por-usuario` se integró en `main` el 22-09-2026 como D103 (secciones 19 y 19b de la verificación).
- 22-09-2026 (D104): el documento 95 (datos en local e instalación propia) describe el sitio tal como está: si cambia algo de lo que afirma (recursos externos que cargan las páginas, qué envía la comunidad, dominios de `analitica.json`, claves de almacenamiento local, contenido que publica `pages.yml`), hay que actualizarlo en ES/EN. Las cifras de documentos de la portada y la entrada siguen escritas a mano (40); `verificar_coherencia.ps1` (22) avisa si dejan de coincidir con la biblioteca. Pendiente del autor: validar el texto del 95 y decidir si quiere que las páginas dejen de cargar las tipografías de Google Fonts (hoy es lo único externo que cargan documentos y portadas).

- 21-09-2026 (D91–D94): la entrada ligera «Qué es SEVEN-G» está en el sitio (`SEVEN-G/build/entrada/` → `html/<idioma>/entrada/`; no editar la copia). Si cambian los datos de demostración, recapturar `SEVEN-G_embudo(.|_en).png` y `SEVEN-G_panel.png` y actualizar la tabla «La cartera, caso a caso» de la sección `#panel` (ES/EN, D108: cifras calculadas con `economia.py` del motor sobre `t01_dashboard_data.json`; la sección 13 falla si los casos o las etapas dejan de coincidir); si cambian los recuentos de documentos, plantillas o aplicaciones, actualizar sus cifras (están escritas a mano, como en la portada). Pendiente del autor: validar los textos de la entrada (ES/EN) y decidir si se hace captura del panel en inglés. (El tooltip y el enlace de los códigos que las herramientas escriben en su interfaz los pone desde el 22-09-2026 `codigos.js`, D99.)

- 20-09-2026 (D86): los tres cursos en presentación (empresa, consultor, partner) se generan con `pwsh -File SEVEN-G/build/curso_pptx.ps1` (lo ejecuta `build.ps1`); su guion está en ese script y sus imágenes en `SEVEN-G/build/img_curso/` (capturas del embudo, de T01 y de T17: recapturarlas si cambian los datos de demostración). El PDF de cada curso lo exporta PowerPoint por COM. Pendiente del autor: validar el contenido de los tres cursos, en especial el del partner (no existe programa de socios; si se quiere crear uno, es una decisión nueva que cambia 91 §6.2 y 93 §7).
- 20-09-2026 (D87): pendiente dar selector de tema al panel móvil de T17 (hoy sigue al sistema) y, si se quiere, llevar la barra del sitio a la página de comunidad y al registro de recomendaciones.
- 20-09-2026 (D84): los hitos mensuales del historial de versiones del documento 04 §2.3 (febrero de 2025 a septiembre de 2026) son una reconstrucción razonada de Claude a petición del autor, no un registro exacto. Pendiente que el autor los revise y corrija lo que no encaje con lo que realmente ocurrió.
- 20-09-2026 (D81): confirmar si el autor quiere renombrar también la organización de GitHub `Seachad-TEAM` y el dominio `seachad.github.io`; no se ha tocado porque es infraestructura real fuera de este repositorio (el cambio de nombre de una cuenta de GitHub no lo puede hacer Claude).
- Biblioteca completa en versión 0.1 (ES/EN) desde el 16-09-2026: estado y pendientes del autor en el §7 del análisis (`_trabajo/20260916_Analisis_estado_y_huecos_SEVEN-G.md`).
- Validar con el autor las decisiones "a validar" (D19, D20, D22, D25) y los umbrales marcados "a calibrar".
- Revisión jurídica del documento 34 (Reglamento (UE) 2026/1744 contra el DOUE; transposición de NIS2; Ley Orgánica de IA en tramitación).
- Confirmar si se pueden publicar enlaces a las demos del panel del consejo (`C:\SEACHAD\AI_en_el_consejo`). El conector T01 → panel vive en `SEVEN-G/herramientas/T17_panel_consejo/` (incluye su propia copia del motor del panel, que desde el 17-09-2026 evoluciona aquí, independiente de `AI_en_el_consejo`); los cambios internos del motor (claves heredadas `estimado_cati`, `aap`; bloques de esferas, madurez e índice) están propuestos en `PROPUESTA_MOTOR.md` de esa carpeta y se aplican en el proyecto privado de origen del motor.
- Publicación: el sitio público es este repositorio (portada `index.html` / `en/index.html` + SEVEN-G y SPHERES generados) mediante `.github/workflows/pages.yml`; alternativa `SEVEN-G/build/publicar.ps1`. Nada de SEVEN-G se publica desde `AI_en_el_consejo`.
- 17-09-2026: `https://seachad.github.io/AI_CONSULTING/` muestra aún la página Jekyll del README: en `origin/main` no están `index.html`, `en/` ni `pages.yml` (commits locales sin subir) y Pages parece configurado como "Deploy from a branch". El autor debe hacer push y poner *Settings → Pages → Source: GitHub Actions*.
- SPHERES v0.1 (D42): validar con el autor el contenido de los seis documentos y si SPHERES debe tener también plantillas o herramientas propias. SPAD (D62, D70): biblioteca completa 00–08 (ES/EN); fuentes anteriores en `SPAD/_legacy/`. Pendiente del autor: regenerar las láminas 5 y 6 del PDF ilustrado con «IA revisora» y decidir si se hacen ilustraciones en inglés (hoy las EN muestran las láminas en español con nota); herramientas propuestas en el documento 08 §7 (validador de artefactos, plantillas de contextos, calculadora de métricas, casos de aplicación). Las ilustraciones se regeneran con `pwsh -File SPAD/build/ilustraciones.ps1` (Windows PowerShell 5.1 para el renderizado).
- T01 y T17 (D43–D45): `registro.html` se genera con `pwsh -File SEVEN-G/herramientas/T01_registro_iniciativas/build_registro.ps1` (la aplicación se edita en `_fuentes/registro.plantilla.html`, nunca en `registro.html`) y el panel de ejemplo con `uv run python t01_a_panel.py` desde `SEVEN-G/herramientas/T17_panel_consejo/`. Si cambian `datos_demo.json` o `config_panel.json`, regenerar ambos; si el motor cambia de versión, actualizar `PANEL_DEMO` en la plantilla y los enlaces de `T17_panel_consejo/index.html`. Validar con el autor la correspondencia fase → etapa del embudo y los límites de días de `config_panel.json` (D44).
- Al pasar a la versión 1.x: retirar el aviso «Versión en revisión: no difundir» (D58) de la portada (`index.html`, `en/index.html`), de los documentos 00 de SEVEN-G y SPHERES (ES/EN), del índice que escribe `SEVEN-G/build/build.ps1` y su comprobación de `verificar_coherencia.ps1`. Hasta entonces, el trabajo en curso es adecuar documentos y herramientas para que sean reutilizables.
- Lectura por capas y curso (D75, D77, D79): cada documento numerado de SEVEN-G lleva `<!-- esencial: nivel | texto -->` antes de su primera sección y su nivel debe coincidir con la tabla del documento 94 §6 (lo comprueba 1j); un documento nuevo necesita su directiva, su fila en el 94 (ES/EN) y, si procede, su fila en 00 §10. El curso vive en `SEVEN-G/mds/<idioma>/curso/` (M00 guía y M01–M09): no define reglas, enlaza a los documentos; si cambia un documento o una herramienta que un módulo recorre, se revisa el módulo. Pendiente del autor: validar los niveles de la matriz (94 §6–§8), los quince disparadores y el contenido del curso; decidir si el curso tendrá versión interactiva (progreso y preguntas en el navegador) y si se registra la marca SEVEN-G (consulta jurídica, no lo cubre la licencia).
- Comunidad (D80): **en servicio desde el 21-09-2026**. Página `SEVEN-G/herramientas/comunidad/` → Cloudflare Worker `seven-g-comunidad` (cuenta de Cloudflare del autor, `https://seven-g-comunidad.pecomac.workers.dev`, registros del Worker desactivados a propósito: no se guardan metadatos de las peticiones) → repositorio público `seachad/seven-g-feedback`. El secreto `GITHUB_TOKEN` del Worker es un token de grano fino (solo ese repositorio, «Issues: Read and write») que **caduca el 20-12-2026**: antes de esa fecha el autor genera otro en GitHub y lo sustituye en *Settings → Variables and Secrets* del Worker; si caduca, la página deja de aceptar envíos y votos (la lista sigue viéndose). El token nunca se escribe en el repositorio. Si cambia `worker/worker.js`: pasar `_pruebas/prueba_worker.html` y volver a pegarlo en el editor del Worker (no hay despliegue automático). Umbral de votos: 50 (`umbralVotos` en el bloque `config` de la página; D101).
- Olas 2 y 3 de herramientas (documento 03 §6).
- D63 (iniciativas transversales y plataformas habilitadoras): añadir al glosario 02 (ES/EN) «iniciativa transversal», «plataforma habilitadora», «escalera de medición por unidad» y «umbral de adopción», y su traducción a `guia_traduccion_en.md` (no se hizo el 18-09-2026 porque otra sesión tenía cambios sin publicar en el 02). Validar con el autor el umbral de adopción del 60 % del ejemplo.
- 18-09-2026: inventario de artefactos de trabajo y huecos en `SEVEN-G/mds/es/_trabajo/20260918_Inventario_de_artefactos_y_huecos_SEVEN-G.md` Resuelto el 19-09-2026 con D67 (plantillas editables en Word, `SEVEN-G/build/docx.ps1`, que `build.ps1` ejecuta) y D68 (plantillas P32–P71, T11 con T13 y T15). Los demás pendientes se cerraron con D69 (sobres, plan de realización en G3.05/G4.11, regla de proveedores en 36, perfil F9 y umbral de alfabetización, alcance del proveedor de alto riesgo). Pendiente del autor: **revisión jurídica de P47, P48, P49, P51 y P56** antes de ofrecerlas a una compañía, y calibrar el umbral de renovación de la alfabetización (PER-PC ≥ 90 %). Si cambia el cuestionario del documento 11, regenerar T15 con `build_madurez.ps1 -ActualizarCuestionario`. T14 (índice de transformación) disponible desde el 18-09-2026 (D64); se genera con `pwsh -File SEVEN-G/herramientas/T14_indice_transformacion/build_indice.ps1`. Desde el 19-09-2026 (D71), T14 calcula las ocho señales desde el registro T01 (esquema 0.5), T18 es la vista «Consejo» de T01 y el panel T17 muestra la tarjeta del índice (`--indice`, ejemplo en `T17_panel_consejo/ejemplo/t14_indice.json`, que se regenera con `build_indice.ps1 -DesdeT01 … -Exportar …` si cambian los datos de T01). Pendiente: calibrar los umbrales v0.1 del índice con datos reales, tarjeta del índice en el panel móvil y lo que falta de T18 según 62 §12.3 (criterio de cumplimiento, fechas, valoración independiente, historial y alertas).
- 16-09-2026: apareció `SEVEN-G/mds/es/_trabajo/20260916_Especificacion_comun_biblioteca_SEVEN-G.md`, no creado en la sesión de trabajo principal. Confirmar con el autor su origen y si sus parámetros (riesgo, madurez "D10 confirmada", autonomía A0–A3, severidades S1–S4, plantillas P01–P31) son decisiones aprobadas antes de registrarlas. Falta su versión en inglés.

@.claude/seveng_decisiones.md
